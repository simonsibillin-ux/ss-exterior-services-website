import type{MetadataRoute}from"next";import{services}from"./services/data";import{posts,serviceAreas}from"./content";
export default function sitemap():MetadataRoute.Sitemap{const base="https://ssexteriorservices.com.au";const now=new Date();return[{url:base,lastModified:now,changeFrequency:"weekly",priority:1},{url:`${base}/about`,lastModified:now,changeFrequency:"yearly",priority:.7},{url:`${base}/contact`,lastModified:now,changeFrequency:"yearly",priority:.8},{url:`${base}/service-areas`,lastModified:now,changeFrequency:"monthly",priority:.8},{url:`${base}/resources`,lastModified:now,changeFrequency:"weekly",priority:.6},{url:`${base}/privacy-policy`,lastModified:now,changeFrequency:"yearly",priority:.2},...services.map(s=>({url:`${base}/services/${s.slug}`,lastModified:now,changeFrequency:"monthly"as const,priority:.9})),...serviceAreas.map(a=>({url:`${base}/service-areas/${a.slug}`,lastModified:now,changeFrequency:"monthly"as const,priority:.75})),...posts.map(p=>({url:`${base}/resources/${p.slug}`,lastModified:new Date(p.date),changeFrequency:"yearly"as const,priority:.55}))]}
