import type{Metadata}from"next";import Link from"next/link";import{notFound}from"next/navigation";import{Footer, Header, PageHero, QuoteBand}from"../../components";import{serviceAreas}from"../../content";import{priorityServices, services}from"../../services/data";
export function generateStaticParams(){return serviceAreas.map(a=>({slug:a.slug}))}export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const{slug}=await params;const area=serviceAreas.find(a=>a.slug===slug);if(!area)return{};return{title:`Exterior Cleaning ${area.name}`, description:`House washing, roof cleaning, pressure washing and exterior cleaning in ${area.name}. Local service from SS Exterior Services.`, alternates:{canonical:`/service-areas/${slug}`}}}
export default async function Area({params}:{params:Promise<{slug:string}>}){const{slug}=await params;const area=serviceAreas.find(a=>a.slug===slug);if(!area)notFound();const schema={"@context":"https://schema.org","@type":"Service", name:`Exterior Cleaning ${area.name}`, provider:{"@id":"https://ssexteriorservices.com.au/#business"}, areaServed:{"@type":"City", name:area.name}, hasOfferCatalog:{"@type":"OfferCatalog", itemListElement:services.map(s=>({"@type":"Offer", itemOffered:{"@type":"Service", name:s.shortTitle}}))}};return <><Header/><main><PageHero eyebrow={`${area.region} service area`} title={`Exterior Cleaning in ${area.name}`}><p>{area.intro}</p></PageHero><section className="local-area section shell"><article><p className="eyebrow">Local service</p><h2>Methods suited to the property, not generic packages.</h2><p>{area.detail}</p><p>We begin with the surface, coating, access and surrounding environment. That allows us to recommend soft washing, controlled pressure, routine cleaning or a targeted treatment with realistic expectations.</p><h2>Priority services in {area.name}</h2><div className="local-services">{priorityServices.map(s=><Link key={s.slug} href={`/services/${s.slug}`}><span>{s.icon}</span><div><h3>{s.shortTitle}</h3><p>{s.summary}</p></div></Link>)}</div></article><aside><p className="eyebrow">All services</p>{services.map(s=><Link key={s.slug} href={`/services/${s.slug}`}>{s.shortTitle}<span>→</span></Link>)}</aside></section><QuoteBand/><script type="application/ld+json" dangerouslySetInnerHTML={{__html:JSON.stringify(schema)}}/></main><Footer/></>}
